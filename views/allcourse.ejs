<!DOCTYPE html>
<html>
  <head>
    <title>All courses</title>
  </head>
  <body>
    <ul>
      <% allcourse.forEach( (course) => { %>
      <li>
        <a href="/course/?courseId=<%= course.courseId %>"
          ><%= course.courseId %></a
        >
      </li>
      <% }); %>
    </ul>
  </body>
</html>
