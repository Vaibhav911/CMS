<!DOCTYPE html>
<html>
  <head>
    <title>All courses</title>
  </head>
  <body>
    <% course.courseId %>
    <ul>
      <% course.file.forEach( (file) => { %>
      <li><%= file.fileName %></a></li>
      <% }); %>
    </ul>
    <p> These files will be downloadble from next sprint </p>
  </body>
</html>
